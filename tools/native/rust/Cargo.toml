[package]
name = "bili-live-cut-rust"
version = "0.1.15-alpha"
edition = "2021"
description = "BiliLiveCut Rust 加速模块 — O(N**2) 聚类矩阵并行计算"

[lib]
name = "_rust_cluster"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
rayon = "1.10"
