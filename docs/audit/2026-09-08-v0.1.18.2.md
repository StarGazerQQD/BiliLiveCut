# v0.1.18.2-Alpha 版本升级与 PR 验收

本次承接全部审计整改，用户已授权升级版本、提交并推送 PR。原始审计与修复验证见 [整改记录](2026-09-08-remediation.md)，其版本、产物与历史测试值保留为当次证据。

| Phase | 状态 | 范围 |
| --- | --- | --- |
| 1 | COMPLETED | 工作区、远端、全部版本引用与冻结约束 |
| 2 | COMPLETED | 版本、源码基线提交、Portable 冻结、测试和构建验收 |
| 3 | IN_PROGRESS | 最终提交、推送及中文 PR |

Phase 1：本地与远端 main 均为 ec147a188819475a7215cdd6073b8c2c946e53b5。已有 77 个修改文件、18 个新增实施文件来自上一轮整改；用户原有 .tmp 不纳入。GitHub 连接器和联网 Git 检查确认仓库及推送权限。源码未变，沿用同一源码最终验证：1,482 passed、0 skipped、覆盖率 74.40%；Ruff、格式、前端、版本检查及当前源码 wheel 验收通过，release_audit 为 44 PASS、0 WARN、0 FAIL；本轮 git diff --check 再次通过。未配置独立类型检查。

版本表示以 app.__version__ 为真源：应用/Portable 为 0.1.18.2-alpha，展示 V0.1.18.2 Alpha，GitHub 标签格式 v0.1.18.2-Alpha，C 为 0.1.18.2，Rust 为 0.1.18-alpha.2。历史 Changelog、审计报告及旧产物校验值保持原记录；模型 revision/内容身份、依赖与 Schema 编号不随补丁号变动。Fixture 的大小、SHA-256、CRC32 必须基于重新构建的实际文件生成。

Portable 从真实 Git 提交提取业务源码，因此 Phase 2 先生成包含修复和新版本的源码基线提交，再同步冻结身份并构建验证。后续只修改发布元数据与文档，不改写冻结提交中的业务文件。

升级边界：现有 app/db/schema.py 同时检查 Schema 和精确应用版本，0.1.18.1-alpha 数据库不能直接由 0.1.18.2-alpha 打开。此次不迁移、不修改用户 .env、数据库或媒体文件。Docker/Podman 不可用；真实直播、模型、付费 LLM、投稿和通知端到端服务不属于本地替身测试结果。

## Phase 2 验收

源码基线提交为 `5187772a7688d7bc3a48ec2e8c44934ba71a085e`。版本副本和当前 Portable 冻结引用全部一致，旧版 Changelog 保留历史内容。对比该提交，当前所有 Payload 业务文件无差异；模型锁完整 engines 数据与原始基线相同，仅更新顶层发布版本和源码身份。新增 Payload 快捷入口修复，按当前薄入口调用构建器，并由脚本自身路径定位模块。

新版本在 `build/release-v0.1.18.2/source` 独立 Git 副本验证，使用工作区 `.venv/Scripts/python.exe`、独立数据库和存储，`ASR_NO_MODEL_DOWNLOAD=1`。

| 检查 | 命令 | 实际结果 |
| --- | --- | --- |
| 初始版本目标 | `python -m pytest tests/test_version_consistency.py tests/unit/test_cli_basic.py packaging/portable/tests/test_builders.py`（另带隔离目录、禁用缓存及 fail-on-skip） | 31 passed |
| 版本、发布与 Portable 目标 | `python -m pytest tests/test_version_consistency.py tests/test_release_audit.py tests/unit/test_cli_basic.py tests/unit/test_release_tooling.py tests/unit/test_release_artifacts.py packaging/portable/tests/test_builders.py packaging/portable/tests/test_manifest_integrity.py packaging/portable/tests/test_release_fixture.py -p no:cacheprovider --basetemp=…/target1 --fail-on-skip` | 119 passed，6.25 秒 |
| 新版最终全量 | `python -m pytest -p no:cacheprovider --basetemp=…/full1 --fail-on-skip --cov=app --cov-report=term-missing --cov-report=xml --cov-fail-under=50` | **1,482 passed、0 skipped，120.93 秒，覆盖率 74.39%** |
| Ruff 与格式 | `python scripts/run_ruff.py check`；`python scripts/run_ruff.py format` | 全部通过，374 files already formatted |
| 版本及发布检查 | `python scripts/check_version_consistency.py`；`python scripts/release_audit.py --quick` | 全部一致；44 PASS、0 WARN、0 FAIL |
| 前端 | `node scripts/check_frontend_interactions.mjs`；`node scripts/check_configuration_interactions.mjs` | 全部通过 |
| Fixture | `python packaging/portable/build_engine_pack.py --fixture` | 构建、自校验通过，明确保持 fixture 分类；没有下载或分发真实模型 |
| Payload | `python scripts/build_portable_payload.py`（先初始化 MSVC/SDK） | exit 0，220 文件，C/Cython/Rust 三个原生模块齐全，两次构建 SHA-256 一致 |
| wheel | `python -m build --wheel --no-isolation --outdir ../wheel` | 构建成功，元数据版本 0.1.18.2a0 |
| 文件核对 | `git diff --check`；`python build/release-v0.1.18.2/verify_artifacts.py` | 484 个跟踪文件与测试副本一致；Payload 全文件哈希符合 manifest，文本按仓库规则归一化换行后与冻结 Git blob 一致；wheel 源文件逐字节一致 |

验证日志位于 `build/release-v0.1.18.2/`。首次快捷构建暴露旧接口导入错误，已修正；第二次编译缺少 MSVC/SDK 可执行路径，使用现有 `vcvars64.bat` 初始化环境并获准执行后构建成功。初次格式检查仅发现版本加载器混合换行，修正后全仓格式通过。产物核对脚本初次把 Windows 换行误判为内容差异，已按仓库现有规范处理 CRLF/LF，并额外对全部文本核对真实 Git blob；没有跳过哈希或版本校验。

依赖版本没有改变；此前在线审计两份 Portable 运行时锁各 116 个依赖均 clean（日志 `build/audit-20260908/fix-phase7-dependency-audit-network.log`）。项目没有独立类型检查配置。完整 EXE/Full 分发包和真实 Docker 镜像未构建，外部端到端边界仍适用。

| 验证产物 | 字节 | SHA-256 | CRC32 |
| --- | ---: | --- | --- |
| source_payload.zip | 841614 | `1050675aa8337c8ac43fdafff4c7aa044b4bebbe65d9af1a640d30af74da3fa3` | `31d84f1f` |
| bili_live_cut-0.1.18.2a0-cp312-cp312-win_amd64.whl | 714958 | `2c030e818e853954a799b465cd5c58c14481319b2445307e4beb8c6cd31bcba6` | `03f58da1` |
| BiliLiveCut-EnginePack-0.1.18.2-alpha.zip（fixture） | 10864 | `b28f659fb5dfafb34df81b3fbf5d947b182630479a84c005aef9c1de3a545035` | `cdd1dfd3` |

机器可读清单为 `build/release-v0.1.18.2/artifacts.json`。测试与构建后只同步本验收文档，不修改已验证业务源码。
