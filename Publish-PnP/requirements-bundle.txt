# 即插即用版运行时依赖(与 pyproject 的 core + asr + web + llm 对齐)。
# build_bundle.py 会据此把全部 wheel 下载到 vendor/wheels;install 脚本离线安装。

# ---- 核心 ----
httpx>=0.27
pydantic>=2.7
pydantic-settings>=2.3
sqlmodel>=0.0.22
loguru>=0.7
typer>=0.12
rich>=13.7
websockets>=12.0
brotli>=1.1
pyyaml>=6.0
numpy>=1.26

# ---- 语音转写(本地 Whisper) ----
faster-whisper>=1.0

# ---- Web 后台 ----
fastapi>=0.111
uvicorn[standard]>=0.30
jinja2>=3.1

# ---- 大模型(OpenAI 兼容:DeepSeek/通义/Kimi/GLM 等) ----
openai>=1.40

# ---- 模型下载工具(打包阶段用,离线运行其实用不到但一并封装) ----
huggingface_hub>=0.23
